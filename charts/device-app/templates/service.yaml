apiVersion: v1
kind: Service
metadata:
  name: {{ include "device-app.fullname" . }}
  labels:
    {{- include "device-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "device-app.selectorLabels" . | nindent 4 }}
